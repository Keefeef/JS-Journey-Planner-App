const Traveller = function(journeys) {
  this.journeys = journeys;
// };

// Traveller.prototype.getJourneyStartLocations = function() {
//   const journeys = [];
//   this.journeys.startLocation.push();
//
//   });
// return journeys;
// }

Traveller.prototype.getJourneyEndLocations = function () {

// };

// Traveller.prototype.getJourneysByTransport = function (transport) {
//   const method = this.journeys.transport.filter( function (journey) {
//     return journey.transport === transport
//   });
// return method;
// }


Traveller.prototype.getJourneysByMinDistance = function (minDistance) {
// const found = this.journeys.distance.filter(function(journey) {
// return journey.distance > minDistance
//   });
//
// return found;
//
// }

Traveller.prototype.calculateTotalDistanceTravelled = function (distances) {

  // const total = this.journeys.distance.reduce(function(total, distance) {
  //   return total + distance;
  // });
  //
  // return total;
  // }

Traveller.prototype.getUniqueModesOfTransport = function () {

// };


module.exports = Traveller;
